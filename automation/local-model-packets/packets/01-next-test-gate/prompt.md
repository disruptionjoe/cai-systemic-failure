# Dispose the three-case contradiction-visibility candidate

## Boundary

Return one raw Markdown provisional disposition artifact using only the frozen
owner facts below. Do not reopen sources, add a case, invent custody or
authority, infer blame or duty, prescribe a remedy, accept schema, create
receiver work, or request action. This is one final diagnostic exception,
limited to one attempt in this slot. Family lineage:
`SF-LIN-PUBLIC-VISIBILITY-CANDIDATE-DISPOSITION-01`.

## Frozen Owner Facts

- `SF-COMP-VISIBILITY-PATTERN-COLUMBIA-FALSIFIER-0001` preserves a three-case
  candidate only: each bounded record independently preserves decision-
  relevant contradiction evidence that was not kept fully visible as one
  accountable review object.
- The result establishes no recurring custody, authority, disposition, stop,
  validation method, or causal mechanism. Exact missing links remain
  `unknown`, and distinct domain explanations remain sovereign.
- The provisional review-chain screen already contains
  `delegation_visibility_gap`, validation-burden, affected-standing, absorber,
  and correction-route/stop concepts. It remains unaccepted and has five
  positive records.
- A useful result must decide whether contradiction visibility adds a bounded
  inquiry pressure beyond that screen, is absorbed by existing fields, or
  should retire. Case count, aerospace domain, severity, institutional
  plurality, and later investigation are not novelty evidence.

## Required Artifact

Draft `SF-VISIBILITY-CANDIDATE-DISPOSITION-0001.md` with delimited YAML
frontmatter: `disposition_id`, `status: provisional_candidate_disposition`,
`subject: contradiction_visibility`, `disposition`, `claim_posture:
provisional`, `source_material:
SF-COMP-VISIBILITY-PATTERN-COLUMBIA-FALSIFIER-0001`, and `external_action:
none`.

Include title, `Boundary`, `Candidate under test`, and a `Decision seam` with
exactly five bullets labeled `Evidence object`, `Affected standing`,
`Decision-time authority`, `Domain absorber`, and `Operative falsifier`.
Preserve every unsupported link as `unknown`.

Then include `Disposition`, `Novel residue`, `Existing-screen absorber`,
`Unknowns`, `Falsifier`, `Candidate effect`, and `Frontier verification`.

Disposition must be exactly one of `novel_inquiry_pressure`,
`absorbed_by_existing_screen`, or `retire`. The falsifier must name evidence
capable of changing the selected disposition.

Candidate effect must be exactly: `DISPOSITION THE THREE-CASE
CONTRADICTION-VISIBILITY CANDIDATE — NO SCHEMA, REMEDY, ACCEPTANCE,
RECEIVER-WORK, OR EXTERNAL-ACTION CHANGE`.

Return only the finished artifact. Do not use a code fence. Keep it under 650
words.
