# Draft a correction-route capability versus outcome pressure test

## Boundary

Write one complete proposal artifact from only the embedded synthetic evidence.
Do not claim repository access, edit files, invent sources, make a domain
expertise claim, prescribe a remedy, accept the provisional schema, create
follow-on work, or request Joe action. The result is candidate material for
Frontier verification, not owner truth.

## Evidence

<evidence path="topology/SF-SCHEMA-CANDIDATE-0001-review-chain-fields.md#candidate-fields">
The `correction_route_stop_condition` field must name an owner-visible route
and admitted source or explicit synthetic basis showing how it can receive or
surface the relevant contradiction to a party able to correct, escalate, stop,
revise, or expose the assumption. Refuse when the route is only a label or is
shown unable to carry the relevant correction.
</evidence>

<evidence path="topology/SF-CRA-0001-correction-route-authority.md#verdict">
A visible label is insufficient when the admitted packet shows that the route
cannot receive, review, or carry the relevant correction to an accountable
owner. Capability, not guaranteed success, is the decision object.
</evidence>

<evidence path="ROADMAP.md">
Keep the candidate provisional and add another bounded test only if a new
public or synthetic case creates material pressure on a core field, refusal
note, annotation, source gate, or correction-route gate.
</evidence>

<synthetic_fixture id="CRO-FIXTURE-01">
- One bounded synthetic packet names route `R-capable`, shows that it received
  contradiction `C-1`, preserved it without alteration, and surfaced it to
  accountable owner `O-review` before the tested reuse decision.
- The packet shows that `O-review` could review, escalate, stop, or revise the
  reuse. It does not guarantee that the owner would choose correction.
- `O-review` considered `C-1` and chose not to revise the reuse. The fixture
  supplies no evidence that `R-capable` was unable to carry the contradiction
  or that the owner lacked the named review capabilities.
- Draft `D-outcome` marks `correction_route_stop_condition` `FAIL` solely
  because no corrective outcome occurred. Draft `D-capability` marks the route
  gate `PASS` because the admitted packet establishes receipt, preservation,
  accountable delivery, and relevant capability while preserving uncertainty
  about the owner's decision and outcome.
- Every non-route core gate is supplied as `PASS` for this exact logic test.
  Those supplied grades are not real findings, a new positive record, or
  schema acceptance.
- All names and facts are synthetic. No real system, source, person,
  organization, remedy, domain conclusion, or external action is represented.
</synthetic_fixture>

## Work now

Draft the finished Markdown artifact
`SF-CRO-0001-correction-route-capability-outcome.md` with exactly these
sections:

1. YAML frontmatter containing `test_id: SF-CRO-0001`,
   `candidate: SF-SCHEMA-CANDIDATE-0001`,
   `status: synthetic_correction_route_capability_proposal`,
   `source_material: synthetic`, `external_action: none`.
2. `# SF CRO 0001 - Correction Route Capability And Outcome`.
3. `## Boundary` preserving proposal-only, provisional, synthetic,
   no-remedy, no-promotion, no-domain-expertise, uncertainty, no-new-record,
   and no-action status.
4. `## Synthetic Fixture` faithfully separating `R-capable`, `C-1`,
   `O-review`, the non-corrective decision, `D-outcome`, and `D-capability`.
   Do not infer a remedy, successful outcome, or real owner authority.
5. `## Capability And Outcome Ledger` comparing contradiction receipt,
   preservation, accountable delivery, relevant capability, actual outcome,
   route-gate result, and uncertainty for both drafts.
6. `## Candidate Gate Trace` with these nine rows in this order:
   `assumption_source_context`, `accountable_review_owner`,
   `affected_system_and_standing`, `changed_condition`, `validation_burden`,
   `observation_environment`, `delegation_visibility_gap`,
   `absorber_or_counterevidence`, `correction_route_stop_condition`.
   Give every non-target row `PASS` with the literal reason `supplied premise`.
   Give `correction_route_stop_condition` `PASS` because the fixture admits
   receipt, preservation, accountable delivery, and relevant capability. Do
   not regrade capability from the owner's later decision or outcome.
7. `## Nearby-Control Comparison` keeping route visibility, route capability,
   accountable-owner decision, and successful correction distinct. State
   whether current wording already makes capability rather than guaranteed
   success the gate's decision object.
8. `## Verdict` choosing exactly one:
   - `PROPOSE SF-CRO-0001` only if current controls require a successful
     corrective outcome before a capable route can pass; or
   - `ALREADY HANDLED — ROUTE CAPABILITY DOES NOT GUARANTEE A CORRECTIVE OUTCOME`
     when current wording already distinguishes capability from success.
9. `## Candidate Effect` containing exactly `NONE — CURRENT CONTROL ALREADY
   DISTINGUISHES CAPABILITY FROM OUTCOME` when the verdict is already handled.
   Do not propose outcome, enforcement, or governance wording.
10. `## Frontier Verification` listing only exact evidence, supplied-premise,
    receipt, preservation, delivery, capability, outcome, qualification,
    wording, and boundary checks needed before integration.

Return only the finished artifact. Emit it as raw Markdown. Do not wrap the
artifact or its YAML frontmatter in a code fence; the first response line must
be `---` and the frontmatter must end with a second `---` before the title.
