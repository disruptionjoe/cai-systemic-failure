# Retry independent TC and CSC refusals compactly

## Boundary

Return one raw Markdown proposal from only the synthetic fixture below. Do not
claim repository access, invent sources, change fields or status, prescribe a
remedy, create follow-on work, or request action. Family lineage:
`SF-LIN-DECISION-CONTROL-CROSSWALK-01`; repair hypothesis: the prior verbose
packet reached the token limit with zero response bytes.

## Fixture

<synthetic_fixture id="DTCX-TC-CSC-DUAL-REFUSAL-REPAIR-01">
At decision `T0`, draft `D-two` calls changed-condition `C-new` a reuse of
`A-old`, but admitted evidence says `A-old` was retired at `T-2` and supplies no
continued-operation, restoration, or reuse bridge. Contradiction `K-one` is
displayed, routed, and disputed at `T-1`. Owner `O-window` holds relevant review
and stop authority from `T-2` through `T+1`. Validation `V-timely` covers burden
`VB-new` and is owner-visible at `T-1`. Route `R-capable` can carry `K-one` to
`O-window`, but no backed condition keeps qualification stopped while `K-one`
remains unresolved. Every other gate is a supplied `PASS`; do not infer
authority exercise, adequacy beyond supplied scope, duty, remedy, or outcome.
</synthetic_fixture>

## Required artifact

Write `SF-DTCX-TC-CSC-DUAL-REFUSAL-0001.md` with raw frontmatter fields
`test_id: SF-DTCX-TC-CSC-DUAL-REFUSAL-0001`,
`status: synthetic_crosswalk_dual_refusal_proposal`,
`source_material: synthetic`, `schema_effect: none`, and
`external_action: none`; title `# SF Decision-Time TC CSC Dual Refusal 0001`;
then `## Boundary`, `## Synthetic Fixture`, `## Control Results`,
`## Candidate Gate Trace`, `## Verdict`, `## Candidate Effect`, and
`## Frontier Verification`.

Copy these control results exactly:

- `TC: FAIL — A-old was retired before T0 with no continued-use bridge`
- `DRA: PASS — O-window has relevant authority across T0`
- `ORC: PASS — K-one is displayed, routed, and disputed before T0`
- `VBT: PASS — V-timely covers VB-new and is owner-visible before T0`
- `CSC: FAIL — R-capable lacks a backed unresolved-qualification stop condition`

Copy all nine gates in owner order. Only `assumption_source_context` and
`correction_route_stop_condition` fail; the other seven say `PASS — supplied
premise`. End the trace with `Infer no other failed gate.` Verdict:
`REFUSE QUALIFICATION — TC AND CSC FAIL INDEPENDENTLY; DRA, ORC, AND VBT PASS
ONLY AS SUPPLIED SYNTHETIC PREMISES`. Candidate effect: `PROPOSE TC-CSC
DUAL-REFUSAL APPLICATION ONLY — NO FIELD, RECORD, STATUS, ACCEPTANCE, OR REMEDY
CHANGE`. Preserve proposal-only, provisional, synthetic, uncertainty,
no-remedy, no-promotion, no-field-change, no-new-record, no-authority-exercise,
no-duty, no-outcome, and no-action boundaries. Use five verification bullets:
fixture; controls; gates; verdict/effect; frontmatter/boundaries.

Return only the finished artifact. Do not use a code fence. Stay under 650 words.
