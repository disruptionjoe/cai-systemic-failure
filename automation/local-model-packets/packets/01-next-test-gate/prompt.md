# Draft a role-title versus scope-relevant review-authority pressure test

## Boundary

Write one complete proposal artifact from only the embedded synthetic evidence.
Do not claim repository access, edit files, invent sources, make a domain
expertise claim, prescribe a remedy, accept the provisional schema, create
follow-on work, or request Joe action. The result is candidate material for
Frontier verification, not owner truth.

## Evidence

<evidence path="topology/SF-SCHEMA-CANDIDATE-0001-review-chain-fields.md#candidate-fields">
The `accountable_review_owner` field must distinguish the assumption owner
from the owner who held relevant authority during the reuse decision window
and could review, stop, escalate, approve or reject, or revise that reuse.
Refuse if accountability is only a generic organization label. Do not infer
that capability was exercised.
</evidence>

<synthetic_fixture id="ASA-FIXTURE-01">
- Every non-target core gate is supplied as `PASS` for this exact logic test.
- Old assumption `A-old` is reused for bounded subsystem `S-bounded` under
  changed condition `C-new` before decision `D-final` at `T-final`.
- Executive `O-title` holds a senior organization-wide title throughout the
  decision window and may receive general reports and set portfolio policy.
- Operative charter `C-scope` explicitly excludes `S-bounded` reuse-review,
  stop, escalation, approval or rejection, and revision decisions from
  `O-title`'s authority. Those exact powers belong only to role `O-scope`.
- `O-scope` is vacant throughout the decision window. No acting appointment,
  delegation, emergency authority, or other accountable owner is shown.
- Draft `D-title` marks `accountable_review_owner` `PASS` from the senior title
  and general authority. Draft `D-scope` marks it `FAIL` because the charter
  excludes the exact subsystem and decision class.
- Do not infer authority exercise, outcome change, vacancy causation, or a
  remedy. This tests title and general authority versus scope-relevant authority.
- All names and facts are synthetic; this is not a new record or acceptance.
</synthetic_fixture>

## Work now

Draft `SF-ASA-0001-role-title-and-scope-relevant-authority.md` with exactly:

1. YAML frontmatter: `test_id: SF-ASA-0001`,
   `candidate: SF-SCHEMA-CANDIDATE-0001`,
   `status: synthetic_authority_scope_proposal`,
   `source_material: synthetic`, `external_action: none`.
2. `# SF ASA 0001 - Role Title And Scope-Relevant Authority`.
3. `## Boundary` preserving proposal-only, provisional, synthetic, no-remedy,
   no-promotion, no-domain-expertise, uncertainty, no-new-record,
   no-outcome-counterfactual, no-vacancy-causation, no-authority-exercise
   inference, and no-action status.
4. `## Synthetic Fixture` separating `A-old`, `S-bounded`, `C-new`,
   `D-final`, `T-final`, `O-title`, `C-scope`, `O-scope`, `D-title`, and
   `D-scope`; never convert title, report receipt, or general authority into
   scope-relevant decision authority.
5. `## Authority Scope Ledger` comparing role, title, authority source,
   effective time, subsystem scope, decision-class scope, review, stop,
   escalation, approval or rejection, revision, vacancy, relation to reuse,
   target-field result, qualification result, and uncertainty for both drafts.
6. `## Candidate Gate Trace` with these nine rows in order:
   `assumption_source_context`, `accountable_review_owner`,
   `affected_system_and_standing`, `changed_condition`, `validation_burden`,
   `observation_environment`, `delegation_visibility_gap`,
   `absorber_or_counterevidence`, `correction_route_stop_condition`.
   Give each non-target row `PASS` / `supplied premise`; give
   `accountable_review_owner` `FAIL` / `named title lacks authority for the
   bounded subsystem and reuse decision class`. Infer no other failed gate.
7. `## Nearby-Control Comparison` keeping title, generic label, general
   authority, report receipt, operative charter, subsystem scope,
   decision-class scope, vacancy, acting authority, authority exercise, review
   adequacy, correction route, and qualification distinct. State whether
   current wording already requires authority relevant to the exact decision.
8. `## Verdict` choosing exactly one:
   - `PROPOSE SF-ASA-0001 — SCOPE-RELEVANT REVIEW AUTHORITY IS NOT EXPLICIT`
     only if title or out-of-scope authority can pass; or
   - `ALREADY HANDLED — ROLE TITLE DOES NOT ESTABLISH SCOPE-RELEVANT REVIEW AUTHORITY`
     when current wording already requires relevant authority.
9. `## Candidate Effect`: exactly `PROPOSE CLARIFYING accountable_review_owner
   TO REQUIRE AUTHORITY OVER THE BOUNDED SYSTEM AND REUSE DECISION CLASS` for
   `PROPOSE`, or exactly `NONE — CURRENT CONTROL ALREADY REQUIRES AUTHORITY
   RELEVANT TO THE REUSE DECISION UNDER REVIEW` for already handled. Propose no
   assignment, organization design, remedy, acceptance rule, or new field.
10. `## Frontier Verification` listing only exact evidence, role, title,
    charter, time, subsystem and decision scope, each authority, vacancy,
    acting authority, decision window, supplied premises, qualification,
    wording, and boundary checks.

Return only raw Markdown. Do not use a code fence; the first line must be `---`
and frontmatter must end with a second `---` before the title.
