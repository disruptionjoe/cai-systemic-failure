# Draft a correction-route pointer-retrievability pressure test

## Boundary

Write one complete proposal artifact from only the embedded synthetic evidence.
Do not claim repository access, edit files, invent sources, make a domain
expertise claim, prescribe a remedy, accept the provisional schema, create
follow-on work, or request Joe action. The result is candidate material for
Frontier verification, not owner truth.

## Evidence

<evidence path="topology/SF-SCHEMA-CANDIDATE-0001-review-chain-fields.md#candidate-fields">
The `correction_route_stop_condition` field must name an owner-visible route
and an admitted basis showing how it can receive or surface the relevant
contradiction to a party able to correct, escalate, stop, revise, or expose the
assumption. Refuse a route that is only a label or cannot carry the relevant
correction.
</evidence>

<evidence path="topology/SF-CRA-0001-correction-route-authority.md#verdict">
A route need not guarantee success. It must make the relevant contradiction
available through an accountable path; delayed, contested, imperfect, or
unresolved outcomes remain graded uncertainty.
</evidence>

<synthetic_fixture id="RPR-FIXTURE-01">
- Every non-target core gate is supplied as `PASS` for this exact logic test.
- Old assumption `A-old` is reused under changed condition `C-new` for bounded
  system `S-bounded` before final decision `D-final`.
- Evidence packet `E-full` identifies `A-old`, `C-new`, `S-bounded`, synthetic
  basis `SRC-full`, and the relevant contradiction.
- Route `R-pointer` stores immutable `E-full` and sends notice `N-pointer`
  before `D-final`. The notice includes stable identifier `REF-full` but does
  not inline the full packet.
- Recipient `O-capable` visibly receives `N-pointer`, can retrieve exact
  `E-full` and `SRC-full` through `REF-full` before `D-final`, and has relevant
  correction, escalation, stop, revision, or exposure capability for
  `S-bounded`. Retrieval is verified by receipt `REC-fetch`.
- Draft `D-inline` marks `correction_route_stop_condition` `FAIL` solely
  because `N-pointer` does not inline the full contradiction. Draft
  `D-surface` preserves the same facts and marks the field `PASS` because the
  route surfaces the exact retrievable packet to `O-capable` in time.
- Do not infer that capability was exercised, the recipient agreed, the route
  succeeded, an outcome followed, inline carriage is preferable, or a remedy
  is required. This tests exact retrievability versus inline payload identity.
- All names and facts are synthetic; this is not a new record or acceptance.
</synthetic_fixture>

## Work now

Draft `SF-RPR-0001-correction-route-pointer-retrievability.md` with exactly:

1. YAML frontmatter: `test_id: SF-RPR-0001`,
   `candidate: SF-SCHEMA-CANDIDATE-0001`,
   `status: synthetic_route_retrievability_proposal`,
   `source_material: synthetic`, `external_action: none`.
2. `# SF RPR 0001 - Correction Route Pointer Retrievability`.
3. `## Boundary` preserving proposal-only, provisional, synthetic, no-remedy,
   no-promotion, no-domain-expertise, uncertainty, no-new-record,
   no-recipient-agreement inference, no-authority-exercise inference,
   no-route-success inference, no-inline-payload preference,
   no-outcome-or-adequacy inference, and no-action status.
4. `## Synthetic Fixture` separating `A-old`, `C-new`, `S-bounded`,
   `D-final`, `E-full`, `SRC-full`, `R-pointer`, `N-pointer`, `REF-full`,
   `O-capable`, `REC-fetch`, `D-inline`, and `D-surface`; never treat the
   summary notice alone as the contradiction or ignore verified retrieval.
5. `## Route Retrieval Ledger` with exactly two rows, one per draft, and
   columns for draft, evidence packet, route, delivered notice, stable
   identifier, recipient, notice timing, recipient visibility, relevant
   capability, inline contradiction, retrievable contradiction, retrieval
   receipt, retrieval timing, draft-claimed target result, verified target
   result, qualification result, and uncertainty. Use identical supplied facts
   in both rows and vary only the draft interpretation. The verified target
   result is `PASS` for both rows; do not infer authority exercise or outcome.
6. `## Candidate Gate Trace` with these nine rows in order:
   `assumption_source_context`, `accountable_review_owner`,
   `affected_system_and_standing`, `changed_condition`, `validation_burden`,
   `observation_environment`, `delegation_visibility_gap`,
   `absorber_or_counterevidence`, `correction_route_stop_condition`.
   Give each non-target row `PASS` / `supplied premise`; give
   `correction_route_stop_condition` `PASS` / `R-pointer surfaces exact E-full
   and SRC-full through retrievable REF-full to O-capable before D-final`.
   Infer no failed gate.
7. `## Nearby-Control Comparison` keeping notice delivery, inline payload,
   stable identity, exact retrievability, recipient visibility, relevant
   capability, authority exercise, route success, outcome, adequacy,
   qualification, uncertainty, and remedy distinct. State whether `receive or
   surface` permits a verified retrievable reference without full inline text.
8. `## Verdict` choosing exactly one:
   - `PROPOSE SF-RPR-0001 — ONLY INLINE CONTRADICTION TEXT CAN SATISFY THE CORRECTION ROUTE`
     only if `D-inline` is required by current wording; or
   - `ALREADY HANDLED — A VERIFIED RETRIEVABLE REFERENCE CAN SURFACE THE RELEVANT CONTRADICTION`
     when current wording permits `D-surface`.
9. `## Candidate Effect`: exactly `PROPOSE CLARIFYING
   correction_route_stop_condition TO ALLOW VERIFIED EXACT RETRIEVABILITY`
   for `PROPOSE`, or exactly `NONE — CURRENT RECEIVE-OR-SURFACE WORDING DOES
   NOT REQUIRE THE FULL CONTRADICTION INLINE` for already handled. Propose no
   route redesign, retention policy, owner action, remedy, acceptance rule,
   annotation, or new field.
10. `## Frontier Verification` listing only exact evidence, identifiers,
    notice and reference, stored packet, recipient visibility, capability,
    retrieval receipt and timing, inline and retrievable content, supplied
    premises, qualification, wording, and boundary checks.

Return only the finished artifact. Emit raw Markdown without a code fence; the
first line must be `---` and frontmatter must end with a second `---` before
the title. Use one concise row or bullet per required item, do not repeat
evidence, and keep the finished artifact under 1,300 words.
