# Draft a correction-route payload-fidelity pressure test

## Boundary

Write one complete proposal artifact from only the embedded synthetic evidence.
Do not claim repository access, edit files, invent sources, make a domain
expertise claim, prescribe a remedy, accept the provisional schema, create
follow-on work, or request Joe action. The result is candidate material for
Frontier verification, not owner truth.

## Evidence

<evidence path="topology/SF-SCHEMA-CANDIDATE-0001-review-chain-fields.md#candidate-fields">
The `correction_route_stop_condition` field must name an owner-visible route
and an admitted basis showing how it can receive or surface the relevant
contradiction to a party able to correct, escalate, stop, revise, or expose the
assumption. Refuse a route that is only a label or cannot carry the relevant
correction.
</evidence>

<evidence path="topology/SF-CRA-0001-correction-route-authority.md#verdict">
A route need not prove success, but it must be able to carry the relevant
contradiction through an accountable path. Imperfect, delayed, contested, or
unresolved outcomes remain graded uncertainty rather than automatic failure.
</evidence>

<synthetic_fixture id="RPF-FIXTURE-01">
- Every non-target core gate is supplied as `PASS` for this exact logic test.
- Old assumption `A-old` is reused under changed condition `C-new` for bounded
  system `S-bounded` before final decision `D-final`.
- Contradiction packet `E-full` identifies `A-old`, `C-new`, `S-bounded`, its
  synthetic evidence basis `SRC-full`, and why the contradiction matters.
- Route `R-summary` receives `E-full` under receipt `REC-full` before
  `D-final`. Its fixed carriage rule `P-summary` strips the assumption,
  changed condition, system, evidence basis, and contradiction content, then
  sends only notice `N-generic`: `a concern was received`.
- Recipient `O-capable` visibly receives `N-generic` before `D-final` and has
  relevant correction, escalation, stop, revision, and exposure capability
  for `S-bounded`. The recipient does not receive `E-full`, `SRC-full`, or an
  identifier that can retrieve them.
- Draft `D-receipt` claims `correction_route_stop_condition` `PASS` from route
  receipt, delivery, visibility, and recipient capability alone. Draft
  `D-fidelity` preserves the same facts and marks the field `FAIL` because the
  route cannot carry or surface the relevant contradiction to `O-capable`.
- Do not infer route outage, recipient disagreement, authority exercise,
  outcome, misconduct, adequacy, or a remedy. This tests generic-notice
  delivery versus contradiction-carriage fidelity.
- All names and facts are synthetic; this is not a new record or acceptance.
</synthetic_fixture>

## Work now

Draft `SF-RPF-0001-correction-route-payload-fidelity.md` with exactly:

1. YAML frontmatter: `test_id: SF-RPF-0001`,
   `candidate: SF-SCHEMA-CANDIDATE-0001`,
   `status: synthetic_route_payload_proposal`,
   `source_material: synthetic`, `external_action: none`.
2. `# SF RPF 0001 - Correction Route Payload Fidelity`.
3. `## Boundary` preserving proposal-only, provisional, synthetic, no-remedy,
   no-promotion, no-domain-expertise, uncertainty, no-new-record,
   no-route-outage inference, no-recipient-agreement inference,
   no-authority-exercise inference, no-outcome-or-adequacy inference,
   no-misconduct inference, and no-action status.
4. `## Synthetic Fixture` separating `A-old`, `C-new`, `S-bounded`,
   `D-final`, `E-full`, `SRC-full`, `R-summary`, `REC-full`, `P-summary`,
   `N-generic`, `O-capable`, `D-receipt`, and `D-fidelity`; never treat the
   generic notice as if it carried the stripped contradiction content.
5. `## Route Payload Ledger` with exactly two rows, one per draft, and columns
   for draft, full evidence packet, route, receipt, carriage rule, delivered
   payload, recipient, recipient visibility, relevant capability, assumption
   delivered, changed condition delivered, bounded system delivered, evidence
   basis delivered, contradiction delivered or retrievable, draft-claimed
   target result, verified target result, qualification result, and
   uncertainty. Use the same supplied facts in both rows; vary only each
   draft's interpretation and claimed result. The verified result is `FAIL`
   for both rows.
6. `## Candidate Gate Trace` with these nine rows in order:
   `assumption_source_context`, `accountable_review_owner`,
   `affected_system_and_standing`, `changed_condition`, `validation_burden`,
   `observation_environment`, `delegation_visibility_gap`,
   `absorber_or_counterevidence`, `correction_route_stop_condition`.
   Give each non-target row `PASS` / `supplied premise`; give
   `correction_route_stop_condition` `FAIL` / `R-summary delivers only
   N-generic and cannot carry or surface E-full or SRC-full to O-capable`.
   Infer no other failed gate.
7. `## Nearby-Control Comparison` keeping route receipt, delivery, recipient
   visibility, relevant authority, payload identity, contradiction fidelity,
   retrievability, authority exercise, outcome, adequacy, qualification,
   uncertainty, and remedy distinct. State whether current wording already
   requires carriage of the relevant contradiction rather than any notice.
8. `## Verdict` choosing exactly one:
   - `PROPOSE SF-RPF-0001 — GENERIC NOTICE DELIVERY IS ENOUGH EVEN WHEN THE ROUTE STRIPS THE RELEVANT CONTRADICTION`
     only if `D-receipt` can pass; or
   - `ALREADY HANDLED — THE ROUTE MUST CARRY OR SURFACE THE RELEVANT CONTRADICTION`
     when current wording already requires contradiction carriage.
9. `## Candidate Effect`: exactly `PROPOSE CLARIFYING
   correction_route_stop_condition TO REQUIRE PAYLOAD FIDELITY` for
   `PROPOSE`, or exactly `NONE — CURRENT CONTROL ALREADY REQUIRES THE ROUTE TO
   CARRY OR SURFACE THE RELEVANT CONTRADICTION` for already handled. Propose no
   route redesign, retention policy, owner action, remedy, acceptance rule,
   annotation, or new field.
10. `## Frontier Verification` listing only exact evidence, identifiers,
    route receipt, carriage rule, delivered payload, recipient visibility,
    capability, stripped and delivered facts, retrievability, supplied
    premises, qualification, wording, and boundary checks.

Return only the finished artifact. Emit raw Markdown without a code fence; the
first line must be `---` and frontmatter must end with a second `---` before
the title. Use one concise row or bullet per required item, do not repeat
evidence, and keep the finished artifact under 1,300 words.
