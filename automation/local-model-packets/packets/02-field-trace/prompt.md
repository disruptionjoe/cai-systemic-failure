# Draft a source-revision effect-locality pressure test

## Boundary

Write one complete proposal artifact from only the embedded synthetic evidence.
Do not claim repository access, edit files, invent sources, make a domain
expertise claim, prescribe a remedy, accept the provisional schema, create
follow-on work, or request Joe action. The result is candidate material for
Frontier verification, not owner truth.

## Evidence

<evidence path="topology/SF-SCHEMA-CANDIDATE-0001-review-chain-fields.md#record-qualification-gate">
The admitted packet preserves any known materially superseding or
contradicting revision and grades any direct material contradiction bearing on
a required field.
</evidence>

<evidence path="topology/SF-SCHEMA-CANDIDATE-0001-review-chain-fields.md#candidate-fields">
`assumption_source_context` preserves and dispositions a known later revision,
retirement event, or direct material contradiction that supersedes, narrows,
withdraws, or conflicts with support carrying a required field. The remaining
fields keep their own required content and refusal conditions.
</evidence>

<evidence path="topology/SF-SRL-0001-source-revision-lineage.md#verdict">
An earlier source revision cannot carry qualification when a known later
revision in the admitted packet materially withdraws or contradicts its
support and the record hides that lineage. The earlier revision remains
historical evidence; the lineage conflict is graded explicitly.
</evidence>

<evidence path="topology/SF-IPC-0001-intra-packet-contradiction.md#verdict">
A direct material contradiction in the admitted packet cannot be left
undispositioned while the affected required field is labeled passed. Contested,
narrowed, or unresolved support remains explicit rather than silently passed.
</evidence>

<synthetic_fixture id="REL-FIXTURE-01">
- Revision `R1` contains observation `O-env`, used only to support how evidence
  was made visible in the record's `observation_environment` field.
- Known later revision `R2` expressly withdraws `O-env`. The admitted lineage
  shows no remaining support for that observation-environment claim.
- `R2` expressly states that it does not revise, withdraw, or address the
  record's old-assumption reuse, changed condition, affected standing,
  validation burden, accountable owner, review-chain fragmentation,
  countermodel, or correction-route facts.
- Separate revision-labeled synthetic premises independently supply each of
  those other fields. None depends on `O-env`, `R1`, or the withdrawn claim.
- Draft record `D-hidden` cites `R1`, omits known `R2`, labels
  `observation_environment` `PASS`, and labels every other supplied field
  `PASS`.
- After `R2` is admitted, draft `D-global` marks every field failed because one
  source revision changed one field's support.
- Draft `D-local` preserves `R1` as historical evidence, records the `R2`
  withdrawal, marks only `observation_environment` unresolved or failed on the
  admitted support, and grades the independently supplied fields on their own
  evidence.
- All names and facts are synthetic. No real source, system, measurement,
  person, organization, remedy, domain conclusion, or external action is
  represented.
</synthetic_fixture>

## Work now

Draft the finished Markdown artifact
`SF-REL-0001-source-revision-effect-locality.md` with exactly these sections:

1. YAML frontmatter containing `test_id: SF-REL-0001`,
   `candidate: SF-SCHEMA-CANDIDATE-0001`,
   `status: synthetic_revision_effect_locality_proposal`,
   `source_material: synthetic`, `external_action: none`.
2. `# SF REL 0001 - Source Revision Effect Locality`.
3. `## Boundary` preserving proposal-only, provisional, synthetic,
   no-remedy, no-promotion, no-domain-expertise, uncertainty, and no-action
   status.
4. `## Synthetic Fixture` faithfully separating `R1`, `O-env`, `R2`, the
   independent premises, and the three draft postures. Do not erase `R1` as
   historical evidence or claim `R2` changed fields it expressly did not
   address.
5. `## Revision Effect Ledger` tracing what `R2` withdraws, which support is
   left unresolved, which other fields have independent support, and why
   lineage preservation does not authorize global contamination or selective
   omission.
6. `## Candidate Gate Trace` with these nine rows in this order:
   `assumption_source_context`, `accountable_review_owner`,
   `affected_system_and_standing`, `changed_condition`, `validation_burden`,
   `observation_environment`, `delegation_visibility_gap`,
   `absorber_or_counterevidence`, `correction_route_stop_condition`.
   Give each row `PASS`, `FAIL`, or `UNRESOLVED` plus one fixture-local reason.
   Preserve the `R2` conflict at the field it bears on and grade all separate
   supplied premises locally.
7. `## Nearby-Control Comparison` comparing revision-effect locality with SRL
   lineage omission and IPC admitted contradiction. State the different
   decision object for each. Do not invent a control hierarchy or require every
   later revision to invalidate every field.
8. `## Verdict` choosing exactly one:
   - `PROPOSE SF-REL-0001` if current revision and contradiction wording leaves
     material ambiguity about keeping a later revision's effect attached to
     the exact support it changes; or
   - `DUPLICATE — CURRENT WORDING SUFFICIENT` if the existing requirement to
     preserve and disposition a later revision bearing on a required field,
     together with field-specific required content, already requires locality.
9. `## Candidate Effect` stating whether current owner wording is sufficient
   or needs one narrow revision-effect-locality clarification. Do not add a
   core field, discard historical evidence, settle a source dispute, spread
   `R2` globally, or accept the schema.
10. `## Frontier Verification` listing only exact revision, dependency,
    field-locality, nonduplication, supplied-premise, wording, and boundary
    checks needed before integration.

Return only the finished artifact.
