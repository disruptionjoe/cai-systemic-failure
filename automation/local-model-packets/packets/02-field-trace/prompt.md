# Draft an evidence-existence versus accountable-review-visibility pressure test

## Boundary

Write one complete proposal artifact from only the embedded synthetic evidence.
Do not claim repository access, edit files, invent sources, make a domain
expertise claim, prescribe a remedy, accept the provisional schema, create
follow-on work, or request Joe action. The result is candidate material for
Frontier verification, not owner truth.

## Evidence

<evidence path="topology/SF-SCHEMA-CANDIDATE-0001-review-chain-fields.md#record-qualification-gate">
The changed condition must create a validation burden that sources show as
unmet, disputed, fragmented, or not owner-visible enough. Delegation, review,
or request paths must be material to the contradiction.
</evidence>

<evidence path="topology/SF-SCHEMA-CANDIDATE-0001-review-chain-fields.md#candidate-fields">
The `observation_environment` field must preserve how relevant evidence was
made visible, missed, normalized, routed, or disputed without importing domain
conclusions. The `delegation_visibility_gap` field must show source-supported
fragmentation in distributed review, authority, request paths, or delegated
decisions.
</evidence>

<synthetic_fixture id="ERV-FIXTURE-01">
- Every non-target core gate is supplied as `PASS` for this exact logic test.
- Old assumption `A-old` is reused under changed condition `C-new` before
  decision `D-final` at `T-final`.
- Contradictory observation `E-log` is recorded before `T-final` in silo
  `S-log` with a traceable timestamp and stable identity.
- Accountable reviewer `O-review` has relevant decision authority, but the
  admitted routing and access record shows no path by which `O-review` can
  access, receive, or discover `E-log` before `T-final`.
- After `D-final`, export `X-late` routes the unchanged `E-log` to `O-review`
  at `T-late`. Preserve its later value and do not backdate visibility.
- Draft `D-exists` marks the observation environment and delegation visibility
  passed merely because `E-log` existed before the decision. Draft `D-visible`
  preserves the contemporaneous record but marks the contradiction missed and
  not owner-visible during the decision window.
- Do not infer that the silo caused the outcome, that `O-review` would have
  changed the decision, that later evidence is invalid, or that a remedy is
  required. This tests evidence existence versus accountable-review visibility.
- All names and facts are synthetic; this is not a new record or acceptance.
</synthetic_fixture>

## Work now

Draft `SF-ERV-0001-evidence-existence-and-review-visibility.md` with exactly:

1. YAML frontmatter: `test_id: SF-ERV-0001`,
   `candidate: SF-SCHEMA-CANDIDATE-0001`,
   `status: synthetic_evidence_visibility_proposal`,
   `source_material: synthetic`, `external_action: none`.
2. `# SF ERV 0001 - Evidence Existence And Review Visibility`.
3. `## Boundary` preserving proposal-only, provisional, synthetic, no-remedy,
   no-promotion, no-domain-expertise, uncertainty, no-new-record,
   no-silo-causation, no-outcome-counterfactual, no-later-evidence-invalidity,
   no-backdating, and no-action status.
4. `## Synthetic Fixture` separating `A-old`, `C-new`, `E-log`, `S-log`,
   `O-review`, `D-final`, `T-final`, `X-late`, `T-late`, `D-exists`, and
   `D-visible`; never convert evidence existence, storage, or later routing
   into decision-time review visibility.
5. `## Visibility Timing Ledger` comparing draft, evidence identity, record
   time, storage location, access path, routing event, accountable reviewer,
   visibility at `T-final`, visibility at artifact writing, target-field
   results, qualification result, and uncertainty for both drafts.
6. `## Candidate Gate Trace` with these nine rows in order:
   `assumption_source_context`, `accountable_review_owner`,
   `affected_system_and_standing`, `changed_condition`, `validation_burden`,
   `observation_environment`, `delegation_visibility_gap`,
   `absorber_or_counterevidence`, `correction_route_stop_condition`.
   Give each non-target row `PASS` / `supplied premise`; give
   `observation_environment` `FAIL` / `evidence existed but was not visible to
   the accountable review chain during the decision window`; give
   `delegation_visibility_gap` `PASS` / `the admitted access and routing record
   preserves the visibility fragmentation`. Infer no other failed gate.
7. `## Nearby-Control Comparison` keeping evidence identity, existence,
   recording, storage, access, routing, owner authority, decision-time
   visibility, later visibility, authority exercise, validation, correction
   route, qualification, and remedy distinct. State whether current wording
   already prevents evidence existence from substituting for accountable-
   review visibility.
8. `## Verdict` choosing exactly one:
   - `PROPOSE SF-ERV-0001 — EVIDENCE EXISTENCE CAN SUBSTITUTE FOR REVIEW VISIBILITY`
     only if a contemporaneous but inaccessible record can pass; or
   - `ALREADY HANDLED — EVIDENCE EXISTENCE DOES NOT ESTABLISH ACCOUNTABLE-REVIEW VISIBILITY`
     when current wording already preserves how evidence was missed or routed.
9. `## Candidate Effect`: exactly `PROPOSE CLARIFYING observation_environment
   TO REQUIRE DECISION-WINDOW VISIBILITY TO THE ACCOUNTABLE REVIEW CHAIN` for
   `PROPOSE`, or exactly `NONE — CURRENT CONTROL ALREADY DISTINGUISHES EVIDENCE
   EXISTENCE FROM ACCOUNTABLE-REVIEW VISIBILITY` for already handled. Propose
   no access system, routing design, owner assignment, remedy, acceptance rule,
   or new field.
10. `## Frontier Verification` listing only exact evidence, record and decision
    times, storage, access, routing, reviewer authority, decision-time and later
    visibility, supplied premises, qualification, wording, and boundary checks.

Return only the finished artifact. Emit it as raw Markdown. Do not use a code
fence; the first line must be `---` and frontmatter must end with a second
`---` before the title.
